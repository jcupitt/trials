<% provide(:title, 'All trials') %>

<%= will_paginate if @trials.count > 30 %>

<ul class="trials">
    <%= render @trials %>
</ul>

<%= will_paginate if @trials.count > 30 %>

<% if is_admin? %>
    <%= link_to "New trial", new_trial_path, class: "btn btn-lg btn-primary" %>
<% end %>
