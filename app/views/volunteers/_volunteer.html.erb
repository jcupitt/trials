<li id="volunteer-<%= volunteer.id %>">
    <span class="trial"><%= link_to volunteer.trial.name, volunteer.trial %></span>
    <span class="user"><%= link_to volunteer.user.name, volunteer.user %></span>
    <span class="notes"><%= link_to volunteer.notes, volunteer %></span>
    <span class="timestamp">
        <%= time_tag(volunteer.created_at) %>
    </span>
</li>
